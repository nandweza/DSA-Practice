# operations and built in functions

myDict = {'name': 'Allan', 'age': 26, 'Profession': 'Programmer', 'hobby': 'Adventuring'}

for key in myDict:
    print(sorted(myDict, reverse=True))

#returns true if all values are true, else otherwise
newDict = {}
print(any(newDict))